现在每次一个需求做完：要执行代码审查（waap-cr）、影响范围分析（impact-analysis）、L3层知识库更新（kb-update），完全靠人自觉；
我个人看了下三个 skill，都是基于变更 patch，打算写一个 req-done 作为需求结束统一触发的 skill：
大概的想法是这个 skill 来统一生成生成 patch 文件：kb-update 应该还有change-files，可以直接用他的脚本（先确认三个要的 patch 是不是一样的，是否能共用）
然后使用claude无头模式串行或者并行执行三个 skill：这个要写成一个脚本，入参要可传 skill 名称，这样并行就 sh 三次，串行就执行完一在执行另一个，根据 patch 的大小自行判断；

也就是说，用户需求完成，会调用这个 skill，然后最终可以看到：代码审查报告、影响范围分析报告、更新好的知识库，主 agent 在基于结果给用户汇报。好好设计下怎么写，可能还要改动到原有的 skill，好好研究下；
