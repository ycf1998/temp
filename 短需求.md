# open-spec-fast

## 小需求正例（走快速通道完整流程）

### 第一类：简单功能

- **新增简单只读查询接口**
  - 判断标准：
    - 仅查询已有数据表，无写操作
    - 逻辑简单（单表或有限关联，无复杂聚合/子查询）
    - 不修改原有其他代码
  - 核心动作：分析需求 → API 设计 → 测试用例 → 测试自动化代码 → 代码实现 → 测试绿灯
  - 理由：需要设计但范围小

- **修改查询接口（向后兼容扩展）**
  - 判断标准：
    - 增加可选入参（有默认值）或增加返回字段（来自已有数据）
    - 原有调用方行为不变
  - 核心动作：分析需求 → 影响评估 → 补充测试用例 → 补充测试自动化代码 → 代码实现 → 测试绿灯
  - 理由：只增不改，风险低

- **修改/增加简单配置**
  - 判断标准：
    - 改动 ≤ 5 行
    - 不涉及逻辑变更
    - 例如：修改常量值、新增枚举值、调整超时/重试次数、改错误码、改文案
  - 核心动作：分析需求 → 影响评估 → 测试建议（手动验证或简单自动化） → 代码实现 → 验证通过
  - 理由：改动极小

### 第二类：下架删除

- **删除功能（下架接口/方法/模块）**
  - 判断标准：
    - 删除代码
    - 需确认无调用方或调用方已全部迁移
  - 核心动作：分析需求 → 影响评估 → 识别并删除/调整受影响的测试用例及自动化测试代码 → 代码实现（删除代码） → 回归测试（测试绿灯）
  - 理由：不需要设计，但必须确保安全

- **删除参数/字段（接口入参、出参、配置项）**
  - 判断标准：
    - 删除未使用的入参/出参/配置项
    - 需确认无任何地方依赖
  - 核心动作：分析需求 → 影响评估 → 识别并删除/调整受影响的测试用例及自动化测试代码 → 代码实现（删除代码） → 回归测试（测试绿灯）
  - 理由：同上

### 第三类：简单Bug修复

- **简单Bug修复（空指针、条件错误、异常缺失等）**
  - 判断标准：
    - 改动 ≤ 30 行
    - 影响文件 ≤ 2 个
    - 修复逻辑直接
  - 核心动作：分析需求 → 定位问题 → 补充测试用例（复现bug） → 代码修复 → 测试绿灯
  - 理由：问题明确，无需完整设计
 

## 快速通道设计文档

**单号**：{{TICKET_ID}}
**匹配正例**：
- 一级大类：{{简单功能 / 下架删除 / 简单Bug修复}}
- 二级正例：{{具体正例名称}}

**需求摘要**：{{用2-4句话描述要做什么、不做什么、关键依赖}}

---

### {{根据匹配的二级正例，输出以下对应部分}}

#### 二级正例：新增简单只读查询接口（属于“简单功能”）

**API设计**
- 路径：{{method}} {{path}}
- 请求参数：{{参数名、类型、必填/可选、说明}}
- 响应结构：{{JSON或类型定义}}
- 错误码：{{如有特殊错误码说明}}

**测试用例说明**（描述式）：
- 需要新增以下测试用例：
  - 正常请求，返回正确数据
  - 请求的资源不存在，返回友好错误码
  - 必填参数缺失，返回参数校验错误

**实现要点**：
- {{例如：复用xxx服务，无需新增表等}}

---

#### 二级正例：修改查询接口（向后兼容扩展）（属于“简单功能”）

**影响评估**
- 变更类型：增加可选入参 / 增加返回字段
- 受影响的接口/方法：{{列出}}
- 兼容性结论：原有调用方无需修改，行为不变

**测试用例说明**：
- 需要补充以下测试用例：
  - 传入新参数时的正常场景
  - 不传新参数时的回退场景
- 现有测试用例无需修改

**实现要点**：
- {{需要修改的文件和方法}}

---

#### 二级正例：修改/增加简单配置（属于“简单功能”）

**影响评估**
- 配置项名称：{{名称}}
- 当前值 → 新值：{{old}} → {{new}}
- 使用位置：{{列出引用该配置的代码位置}}
- 影响范围：{{描述}}

**测试建议**（手工或简单自动化）：
- 验证配置生效的方法
- 回归验证原有行为未受影响的方法

**实现要点**：
- {{需要修改的文件和行号}}

---

#### 二级正例：删除功能（下架接口/方法/模块）（属于“下架删除”）

**影响评估**
- 删除对象：{{接口/方法/模块}}
- 静态分析结果：{{列出调用方位置，或确认无调用}}
- 安全性结论：{{确认可安全删除，或需先迁移}}

**受影响的现有测试用例及自动化代码**：
- 需要删除的测试用例：{{列出}}
- 需要修改的测试用例：{{列出及修改原因}}
- 需要删除的自动化代码：{{列出}}

**回归测试范围**：{{建议运行哪些测试套件}}

**实现要点**：
- {{需要删除的代码位置}}

---

#### 二级正例：删除参数/字段（属于“下架删除”）

**影响评估**
- 删除对象：{{入参/出参/配置项}}
- 静态分析结果：{{确认无任何依赖}}
- 安全性结论：{{可安全删除}}

**受影响的现有测试用例及自动化代码**：
- 需要删除/修改的测试用例：{{列出}}
- 需要删除的自动化代码：{{列出}}

**回归测试范围**：{{建议运行哪些测试套件}}

**实现要点**：
- {{需要删除的代码位置}}

---

#### 二级正例：简单Bug修复（属于“简单Bug修复”）

**定位问题**
- 问题现象：{{描述}}
- 根因位置：{{文件及行号/函数}}
- 根因描述：{{例如：未对空指针做判断}}

**修复方案**：
- {{改动描述}}

**测试用例说明**：
- 需要新增一个能复现该 Bug 的测试用例（输入 {{条件}}，期望 {{正确行为}}）
- 如需修改现有测试期望值，列出
- 建议补充相关边界测试场景

**实现要点**：
- {{需要修改的文件和具体改动}}

---

### 确认及下一步流程

请确认以上设计无误。确认后，我将按以下流程执行（针对当前匹配的二级正例）：

{{根据二级正例，列出完整流程步骤}}

**示例**（如果是“新增简单只读查询接口”）：
> 分析需求 → API设计 → 测试用例 → 测试自动化代码 → 代码实现 → 测试绿灯

**示例**（如果是“修改查询接口（向后兼容扩展）”）：
> 分析需求 → 影响评估 → 补充测试用例 → 补充测试自动化代码 → 代码实现 → 测试绿灯

**示例**（如果是“修改/增加简单配置”）：
> 分析需求 → 影响评估 → 测试建议（手动验证或简单自动化） → 代码实现 → 验证通过

**示例**（如果是“删除功能”或“删除参数/字段”）：
> 分析需求 → 影响评估 → 识别并删除/调整受影响的测试用例及自动化测试代码 → 代码实现（删除代码） → 回归测试（测试绿灯）

**示例**（如果是“简单Bug修复”）：
> 分析需求 → 定位问题 → 补充测试用例（复现bug） → 代码修复 → 测试绿灯

回复“确认”开始执行。



flowchart TD
    A[用户输入 /open-spec-fast 单号 + 描述] --> B[Claude 交互式分析需求<br>（边问边澄清，直到理解清晰）]
    B --> C{匹配小需求正例？<br>（对照正例清单逐条判断）}
    C -->|否| D[提示用户：<br>该需求不在快速通道范围内<br>请使用标准 openSpec 流程]
    D --> E[终止]
    
    C -->|是| F[生成快速通道设计文档<br>（根据匹配到的具体正例，<br>按模板填充内容，包括：<br>API设计/影响评估/测试用例说明等）]
    F --> G[用户审核文档]
    G -->|用户提出修改| H[根据反馈修改文档]
    H --> G
    G -->|用户确认| I[全自动执行该正例对应的流程步骤]
    
    I --> J{流程中是否包含<br>影响评估/API设计等前置步骤？}
    J -->|是| K[自动完成影响评估/API设计<br>（无需用户确认）]
    K --> L[自动生成/补充测试用例和测试代码]
    J -->|否| L
    
    L --> M[自动生成实现代码]
    M --> N[自动运行相关测试]
    N --> O{测试是否全部通过？}
    O -->|否| P{重试次数 < 3？}
    P -->|是| Q[自动分析失败原因<br>并修复（代码或测试）]
    Q --> N
    P -->|否| R[报告失败，暂停<br>提示用户人工介入]
    O -->|是| S[输出结果：<br>✅ 所有测试通过<br>修改文件列表<br>生成commit消息]
    S --> T[完成，用户自行push并创建PR/MR]
